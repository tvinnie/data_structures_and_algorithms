import calendar

print(calendar.month(2024,12)) # prints the calendar the month of december, year 2024

# Same operation but without the print funtion, using the prmonth() funtion.

import calendar

calendar.prmonth(2024,12)